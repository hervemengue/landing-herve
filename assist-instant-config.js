/**
 * URL de l'API Assist instantané (Cloudflare Worker).
 * Après déploiement : coller l'URL ici (voir worker/README.md).
 * Exemple : "https://assist-instant-api.herve-mengue.workers.dev/api/generate"
 */
window.ASSIST_INSTANT_CONFIG = {
  apiUrl: "",
};
